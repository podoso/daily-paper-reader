---
title: "WhiteMatter: All-to-All Cross-Layer Connections via KV Mixing"
title_zh: WhiteMatter：通过KV混合实现的全对全跨层连接
authors: "Wenbo Zhang, Xiang Ren"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18486v1"
tags: ["query:llm"]
score: 9.0
evidence: Transformer架构改进，通过KV混合实现跨层全连接
tldr: "Transformer中每层只能关注自身深度产生的KV，忽略了解码时已有的更深层表示。现有反馈架构虽允许浅层使用深层KV，但连接模式固定。WhiteMatter提出全连接跨层机制，通过路由器将每个token的L层状态混合成k个KV通道，供所有注意力层按需选择。在预训练中，它超越层数多50%的Transformer，即使压缩50%KV缓存仍保持大部分性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.18486v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1416, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1430, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1411, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1147, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1422, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1448, \"height\": 617, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18486v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1147, \"height\": 703, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.18486v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18486v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18486v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1451, \"height\": 299, \"label\": \"Table\"}]"
motivation: Transformer各层只能访问自身层级的KV，反馈结构连接模式固定，无法自适应利用深层表示。
method: WhiteMatter用路由器将每token的L层状态混合为k个KV通道，各注意力层按需选择通道，通道数k控制缓存大小。
result: "预训练中WhiteMatter超越层数提升50%的基线，并在KV缓存压缩50%时仍保大部分增益。"
conclusion: WhiteMatter通过全连接跨层KV混合，以可学习路由实现高效记忆共享，兼顾性能与缓存压缩。
---

## 摘要
在Transformer中，每一层仅通过其自身深度处生成的KV来关注过去的令牌，尽管在自回归解码过程中存在更深的表示。反馈架构允许浅层消费层关注由更深的过去令牌表示生成的KV，但为所有消费层提供了相同的固定连接模式到源层。我们提出了WhiteMatter，它将每个注意力层与每个过去令牌的所有层的表示连接起来，连接权重可随消费层变化并适应源令牌。对于每个令牌，路由器通过将其L层状态混合到k个KV通道中来实现这些连接，这些通道被缓存以供后续令牌使用；每个消费层关注其中一个通道。通道数k控制KV缓存大小。设置k<L可减少缓存的内存占用。在我们的预训练实验中，WhiteMatter优于具有50%更多层的普通Transformer，并在50% KV缓存压缩下保留了大部分增益。

## Abstract
In a Transformer, each layer attends to past tokens only through KV produced at its own depth, despite the presence of deeper representations during autoregressive decoding. Feedback architectures allow shallow consumer layers to attend to KV produced by deeper past-token representations, but give all consumer layers the same fixed connection patterns to source layers. We propose WhiteMatter, which connects every attention layer to the representations from all layers of each past token, with connection weights that can vary across consumer layers and adapt to the source token. For each token, a router implements these connections by mixing its $L$ layer states into $k$ KV channels that are cached for subsequent tokens; each consumer layer attends to one of the channels. The number of channels $k$ controls the KV-cache size. Setting $k<L$ reduces the cache's memory footprint. In our pretraining experiments, WhiteMatter outperforms a vanilla Transformer with 50% more layers and retains most of this gain with a 50% KV-cache compression.

---

## 论文详细总结（自动生成）

## 论文核心总结：WhiteMatter（通过KV混合实现全对全跨层连接）

### 1. 核心问题与研究动机

- **背景问题**：在标准 Transformer 中，每一层注意力只能访问**同一深度**为过去 token 生成的 Key/Value（KV），而自回归解码时，过去的 token 实际上已经产生了更深层的表示，这些深层表示未被当前层利用。
- **已有反馈架构的不足**：一些“反馈架构”允许较浅的消费层使用由更深层过去 token 表示生成的 KV，但其连接模式是**固定**的：所有消费层都以相同的方式连接到固定的源层，缺乏灵活性和自适应性。
- **研究目标**：提出一种机制，让**每一层注意力都能访问过去 token 的任意层表示**，并且连接权重可以随消费层不同而变化、随源 token 语义自适应，同时兼顾 KV 缓存的内存开销。

### 2. 方法：WhiteMatter

- **核心思想**：打破“层”之间的隔离，实现 **全对全跨层 KV 连接**。
- **路由器（Router）**：
  - 对每个 token，路由其 \(L\) 层隐藏状态，混合得到 **\(k\) 个 KV 通道（channels）**。
  - 这些通道被缓存，供后续 token 的各注意力层使用。
- **消费层选择**：每个注意力层从 \(k\) 个通道中选择一个进行关注；不同层可以选择不同通道，从而获得不同深度的上下文特征。
- **缓存控制**：通道数 \(k\) 直接控制 KV 缓存大小。当 \(k < L\) 时，与缓存全部 \(L\) 层 KV 相比，可减少内存占用。
- **关键性质**：连接权重不是固定的，而是：
  - 随**消费层**变化；
  - 随**源 token** 自适应调整。

### 3. 实验设计

- **实验场景**：预训练（pretraining）。
- **主要对比**：
  - WhiteMatter 与普通 Transformer（vanilla Transformer）的对比。
  - 重点对比对象是 **层数多 50% 的普通 Transformer**。
  - 额外验证了 **50% KV 缓存压缩** 情况下 WhiteMatter 的表现。
- **Benchmark 与数据集**：
  - 摘要中**未明确说明**使用了哪些具体数据集、语料库或下游任务 benchmark。
  - 也未说明模型参数量、训练 token 数等细节。

### 4. 资源与算力

- 论文摘要中**没有提供具体的算力信息**，例如 GPU 型号、数量、训练时长、模型参数规模等。
- 该信息可能存在于论文正文或实验设置中，但当前给出的提取文本未包含相关内容。

### 5. 实验数量与充分性

- 从摘要可见至少包含两类结果：
  1. 与层数增加 50% 的 Transformer 对比的主结果；
  2. KV 缓存压缩 50% 的实验。
- **充分性评估**：
  - 优点：核心结论（超越更深基线、压缩缓存仍保持收益）清晰，直接对应方法动机。
  - 不足：摘要层面无法看到模型规模、数据规模、训练步数、多种基线（如固定反馈架构）的对比；也未见消融实验（如不同 \(k\) 值、路由器设计等）的说明。因此从当前信息看，实验**覆盖面有限**，公平性需要原文详细实验才能充分判断。

### 6. 主要结论与发现

- WhiteMatter 在预训练中**优于具有 50% 更多层的普通 Transformer**。
- 当 KV 缓存压缩到 50% 时（即更小的 \(k\)），WhiteMatter **仍能保留大部分性能增益**。
- 说明通过可学习路由实现的全对全跨层 KV 混合，既能提升模型能力，又能作为内存高效的缓存压缩手段。

### 7. 优点

- **架构创新性强**：将固定、局部的跨层反馈改为全对全、可学习的 KV 路由，是 Transformer 架构演进中的有价值思路。
- **统一了“记忆共享”与“缓存压缩”**：通道数 \(k\) 是天然的内存控制旋钮，性能和效率可以在一个框架内权衡。
- **自适应连接**：路由权重随消费层和源 token 动态变化，比固定反馈连接更灵活，可能更贴合不同上下文的需求。
- **结果有吸引力**：用更少层数超越更深基线，并支持缓存压缩，具有实际部署潜力。

### 8. 不足与局限

- **实验细节缺失**：摘要未给出数据集、模型规模、计算成本等关键实验信息，难以判断泛化性和可复现性。
- **基线覆盖有限**：只明确提到普通 Transformer；与已有反馈架构、其他缓存压缩方法的对比情况未知。
- **消融不足**：没有展示路由器设计选择、通道数 \(k\) 的影响、不同 token 路由行为分析等消融实验。
- **潜在风险**：
  - 全对全 KV 混合可能引入额外的路由计算开销；
  - 跨层连接可能带来训练稳定性和扩展性问题；
  - 在长序列或超大模型上的效果仍需验证。
- **应用限制**：摘要集中于预训练；下游任务、零样本/少样本能力、指令微调等尚未涉及。

（完）
